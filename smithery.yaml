startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({ command: 'npx', args: ['@underground-cultural-district/spines-underground'] })
